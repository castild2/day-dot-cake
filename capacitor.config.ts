import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'day-dot-cake',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
